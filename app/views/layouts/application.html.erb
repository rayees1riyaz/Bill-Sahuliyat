<!DOCTYPE html>
<html class="h-full bg-gray-50">
  <head>
    <title>Bill Sahuliyat | Professional GST Billing</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Google Fonts: Inter %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">

    <%# Tailwind CSS via CDN %>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      tailwind.config = {
        theme: {
          extend: {
            fontFamily: {
              sans: ['Inter', 'ui-sans-serif', 'system-ui'],
            },
            colors: {
              brand: {
                50: '#f0f4ff',
                100: '#d9e2ff',
                200: '#b3c5ff',
                500: '#3b82f6',
                600: '#2563eb',
                700: '#1d4ed8',
                800: '#1e40af',
                900: '#1e3a8a',
              }
            },
            borderRadius: {
              '3xl': '1.5rem',
              '4xl': '2rem',
            }
          }
        }
      }
    </script>
    
    <style>
      body { font-family: 'Inter', sans-serif; -webkit-font-smoothing: antialiased; }
      .nav-gradient { background: linear-gradient(135deg, #1e3a8a 0%, #2563eb 100%); }
      turbo-frame[busy] #search-spinner { opacity: 1; }
      turbo-frame[busy] #frame-loader { opacity: 1; pointer-events: auto; }
      turbo-frame:not([busy]) #search-spinner { opacity: 0; pointer-events: none; }
      turbo-frame:not([busy]) #frame-loader { opacity: 0; pointer-events: none; }
      
      @keyframes pop-in {
        0% { transform: scale(0.95); opacity: 0; }
        100% { transform: scale(1); opacity: 1; }
      }
      .animate-pop-in { animation: pop-in 0.4s cubic-bezier(0.34, 1.56, 0.64, 1) both; }
    </style>

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "print", media: "print" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-full flex flex-col">
    <header class="no-print sticky top-0 z-50">
      <nav class="nav-gradient shadow-xl">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="flex justify-between h-20 items-center">
            <%# Logo %>
            <%= link_to root_path, class: "flex items-center space-x-3" do %>
              <div class="bg-white p-2 rounded-2xl shadow-inner transform rotate-3">
                <svg class="w-8 h-8 text-brand-700" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M9 7h6m0 10v-3m-3 3h.01M9 17h.01M9 14h.01M12 14h.01M15 11h.01M12 11h.01M9 11h.01M7 21h10a2 2 0 002-2V5a2 2 0 00-2-2H7a2 2 0 00-2 2v14a2 2 0 002 2z"></path>
                </svg>
              </div>
              <div class="flex flex-col">
                <span class="text-white text-xl font-black tracking-tight leading-none uppercase">Bill Sahuliyat</span>
                <span class="text-[9px] text-brand-200 uppercase tracking-widest font-black mt-1 italic">Enterprise Edition</span>
              </div>
            <% end %>
            
            <%# Desktop Nav %>
            <div class="hidden md:flex items-center space-x-8">
              <% if user_signed_in? %>
                <%= link_to "Dashboard", invoices_path, class: "text-brand-50 hover:text-white font-bold text-sm tracking-wide transition-all duration-200" %>
                <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "text-brand-100 hover:text-white font-bold text-sm tracking-wide bg-white/10 px-4 py-2 rounded-xl transition" %>
              <% else %>
                <%= link_to "Sign In", new_user_session_path, class: "text-brand-50 hover:text-white font-bold text-sm tracking-wide transition" %>
                <%= link_to "Get Started", new_user_registration_path, class: "bg-white text-brand-600 px-6 py-2 rounded-xl font-black text-sm uppercase tracking-widest shadow-lg shadow-brand-900/20 active:scale-95 transition" %>
              <% end %>
            </div>

            <%# Mobile Controls (Simplified Header) %>
            <div class="md:hidden flex items-center space-x-2 no-print">
              <% if user_signed_in? %>
                <button class="p-2 text-white/80 hover:text-white transition">
                  <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path></svg>
                </button>
              <% end %>
            </div>
          </div>
        </div>
      </nav>
    </header>

    <div class="fixed top-24 right-4 z-[60] w-full max-w-sm pointer-events-none space-y-3" id="flash-container">
      <%= render "shared/flash" %>
    </div>

    <main class="flex-grow">
      <%= yield %>
    </main>

    <% if user_signed_in? %>
      <footer class="no-print bg-white border-t border-gray-100 py-12 pb-32 md:pb-12">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="flex flex-col md:flex-row justify-between items-center space-y-6 md:space-y-0 text-center md:text-left">
            <div>
              <p class="text-gray-900 font-black uppercase text-sm tracking-widest">Saadat Enterprises</p>
              <p class="text-gray-400 text-xs mt-1 font-medium">Professional Managed Billing & GST Compliance</p>
            </div>
            <div class="text-gray-500 text-xs font-bold uppercase tracking-widest">
              &copy; <%= Time.now.year %> All rights reserved.
            </div>
          </div>
        </div>
      </footer>

      <%# Mobile Bottom Navigation %>
      <div class="md:hidden no-print fixed bottom-0 left-0 right-0 z-[100] bg-white/80 backdrop-blur-xl border-t border-gray-100 px-6 py-4 flex items-center justify-between shadow-[0_-10px_40px_rgba(0,0,0,0.05)] rounded-t-[2.5rem]">
        <%= link_to invoices_path, class: "flex flex-col items-center space-y-1 transition-all duration-300 #{(params[:controller] == 'invoices' && params[:action] == 'index') ? 'text-brand-600 scale-110' : 'text-gray-400 hover:text-gray-600'}" do %>
          <svg class="w-7 h-7" fill="<%= (params[:controller] == 'invoices' && params[:action] == 'index') ? 'currentColor' : 'none' %>" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"></path>
          </svg>
          <span class="text-[10px] font-black uppercase tracking-tighter">Home</span>
        <% end %>

        <div class="relative -mt-12">
          <%= link_to new_invoice_path, class: "flex items-center justify-center w-16 h-16 bg-brand-600 text-white rounded-full shadow-2xl shadow-brand-400 border-4 border-white transition-all duration-500 hover:scale-110 active:scale-90 transform" do %>
            <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M12 4v16m8-8H4"></path></svg>
          <% end %>
        </div>

        <%= button_to destroy_user_session_path, method: :delete, class: "flex flex-col items-center space-y-1 text-gray-400 hover:text-red-600 transition-all duration-300" do %>
          <svg class="w-7 h-7" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path>
          </svg>
          <span class="text-[10px] font-black uppercase tracking-tighter">Logout</span>
        <% end %>
      </div>
    <% end %>
  </body>
</html>
